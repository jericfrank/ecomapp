import React, { Component } from 'react';
import { SideContainer } from '../styles/sidebar';

export default class Sidebar extends Component {
    render() {
        return (
            <SideContainer 
            	padder
            />
        );
    }
}